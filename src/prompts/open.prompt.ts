import type { OpenBrowserType } from '../types/index.js'
// import { findBrowserList } from '../utils/manifest.js'

export async function promptOpenOptions(partial: OpenBrowserType) {
  // let { target, url } = partial
  // const item = target ? (await findBrowserList(target)) : undefined
  // if (!item) {
  //     //
  // }
}
